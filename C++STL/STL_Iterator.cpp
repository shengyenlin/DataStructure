#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    //declare a int vector iterator
    vector<int>::iterator intVecIter;
    //declare a const iterator: prevent the iterator from 
    //modifying the elements of a container
    //process the elements of those vector containers that 
    //are declared as constant vector containers
    vector<int>::const_iterator intConstVecIter;
    return 0;
}